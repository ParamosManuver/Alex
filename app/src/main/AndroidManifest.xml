<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mike.alex">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".HomeActivity"
            android:label="Home" />
        <activity
            android:name=".LoginActivity"
            android:label="login" />
        <activity
            android:name=".CreateActivity"
            android:label="create account" />
        <activity android:name=".AccountActivity"
            android:label="Account details"
            />
        <activity android:name=".SelectLoanActivity"
            android:label="Loan Selection"
            />
        <activity android:name=".PayloanActivity"
            android:label="Loan payment"

            />
        <activity android:name=".ApplyShortActivity"
            android:label="Apply Short term loan"

            />
        <activity android:name=".ApplyLongActivity"
            android:label="Apply Long term loan"

            />
        <activity android:name=".PaylongTermActivity"
            android:label="Pay Long  term loan"
            />
        <activity android:name=".PayshortTermActivity"
            android:label="Pay Short Term Loan">

        </activity>
    </application>

</manifest>